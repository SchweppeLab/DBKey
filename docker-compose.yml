version: "3.0"

services:
  msptodb:
    image: dkschwep/dbkey
    container_name: dbkey
    build:
      context: .
      dockerfile: Dockerfile
    # restart: always
    user: 'root'
    ports:
      - '3838:3838'
    volumes:
      - 'dbkey_logs:/var/log/shiny-server'
      - './mountpoints/apps:/srv/shiny-server'

volumes:
  dbkey_logs:
